{
  "name": "@adaptive-agent/desktop-bridge",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --target bun",
    "compile": "bun build src/main.ts --compile --outfile dist/agent-runtime",
    "test": "bunx vitest run && bun test ./src/sqlite-runtime.bun.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@adaptive-agent/agent-sdk": "workspace:*",
    "@adaptive-agent/core": "workspace:*",
    "@adaptive-agent/gateway-client": "workspace:*"
  },
  "devDependencies": {
    "@adaptive-agent/capability-gateway": "workspace:*",
    "bun-types": "^1.3.5",
    "jose": "^6.1.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
