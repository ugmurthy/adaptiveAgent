{
  "name": "@adaptive-agent/desktop-bridge",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --target bun",
    "compile": "bun build src/main.ts --compile --outfile dist/agent-runtime",
    "test": "bunx vitest run",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@adaptive-agent/agent-sdk": "workspace:*",
    "@adaptive-agent/core": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.5",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
