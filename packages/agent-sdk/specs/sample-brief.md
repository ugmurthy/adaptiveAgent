# Fixture Brief

This markdown file is meant to test `text/markdown` handling.

## Goals

- Confirm the model can read markdown structure.
- Check whether headings and list items survive ingestion.
- Give the runner one more file type beyond plain text.
