# Agent SDK Specs

These specs are intended for the `adaptive-agent` CLI in `packages/agent-sdk`.

Example:

```bash
bun run ./packages/agent-sdk/dist/adaptive-agent.js --spec ./packages/agent-sdk/specs/multimodal-fixture-spec.json
```

The paths inside each spec are resolved relative to the spec file location.
